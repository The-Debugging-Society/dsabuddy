services:
  backend:
    build: ./backend
    container_name: dsabuddy-backend
    ports:
      - "5001:5001"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: dsabuddy-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend